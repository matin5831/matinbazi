services:
  - type: web
    name: matin-bazi
    runtime: node
    plan: free
    buildCommand: npm run build
    startCommand: node server.js
    healthCheckPath: /api/health
    envVars:
      - key: NODE_VERSION
        value: 20
